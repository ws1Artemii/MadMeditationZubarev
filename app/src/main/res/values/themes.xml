<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MadMeditationZubarev" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main</item>
        <item name="colorPrimaryVariant">@color/variant</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="splash_screen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="background">@drawable/background1</item>
        <item name="android:foreground">@drawable/splashscreen_background</item>
    </style>

    <style name="Shapes">

    </style>

    <style name="Shapes.circle">

        <item name="cornerFamily">rounded</item>

        <item name="cornerSize">50%p</item>

    </style>
</resources>